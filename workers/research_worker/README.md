# Research Worker Scaffold

The Research Worker is a Python sidecar subsystem for data profiling, analysis scripts, charts, reports, notebooks, and reproducibility manifests.

Current scaffold:

- no external dependencies;
- standard-library CSV profiler prototype lives in `scripts/prototype_csv_profiler.py`;
- future worker code should emit artifacts with hashes and privacy classes.

